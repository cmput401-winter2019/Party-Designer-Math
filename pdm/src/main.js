import {PartyInterface} from "./scenes/PartyInterface"

var config = {
    type: Phaser.AUTO,
    width: 800,
    height: 600,
    physics: {
    },
    scene: { PartyInterface
    }
};

var game = new Phaser.Game(config);